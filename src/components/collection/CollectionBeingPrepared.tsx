import { Container } from '../pages/layout'

export default function CollectionBeingPrepared() {
    return (
        <Container>
            It can take a few minutes to prepare a collection for tracking. This page will refresh once the collection is ready.
        </Container>
    )
}